# Experiment 9 - Custom Node.js HTTP Server

This experiment demonstrates a custom server using Node.js built-in modules `http`, `os`, `path`, and `events`.

## Run

1. Install dependencies: `npm install`
2. Start: `node server.js`
